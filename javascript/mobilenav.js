window.addEventListener("load", function() {
    // do title disappear - experiment w css transition instead of based on scroll
    // make menu icons go to the side. if exceed certain # of pixels, then use transform. applyy css transition
})